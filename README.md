# tb-xray-detection
Built a deep learning model to detect tuberculosis from chest X-rays. Uses DenseNet121 transfer learning, trained on 4,200 images, and hits 97% accuracy with an AUC of 0.99 on the test set.
